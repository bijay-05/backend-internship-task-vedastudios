export interface productObj {
    id: number;
    images: string[];
    title: string;
    price: number;
    rating: number;
}

export interface categoryObj {
    slug: string;
    name: string;
    url: string
}